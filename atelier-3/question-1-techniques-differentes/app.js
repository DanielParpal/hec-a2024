var quantites = [
	0,
	0, // index -> 1
	0
];

// toutes ces options sont des synonymes
// dans ce cas-ci, c'est à votre discrétion
quantites[1] = quantites[1] + 1; // une première façon
quantites[1] += 1; // une deuxième façon
quantites[1]++; // une troisième

